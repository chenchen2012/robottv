{
  "name": "robot-tv-news-studio",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build:seo": "node scripts/build-static-news-seo.mjs",
    "build:dist": "node scripts/sync-static-seo-to-dist.mjs",
    "build:netlify": "npm run build:seo && npm run build && npm run build:dist",
    "dev": "sanity dev",
    "build": "COLUMNS=120 LINES=40 sanity build",
    "deploy": "sanity deploy"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "sanity": "^3.80.1",
    "styled-components": "^6.1.15"
  }
}
